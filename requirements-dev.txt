# Developer dependencies for this repo (local + CI)
# Keep versions predictable; bump intentionally via PRs.

# Core tooling
pre-commit>=3.7,<5.0
yamllint>=1.35,<2.0

# Security / sanity helpers (commonly used by pre-commit hooks)
ruamel.yaml>=0.17,<0.19
packaging>=23.2,<25.0
