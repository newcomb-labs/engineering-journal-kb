# ---
# name: chatgpt-review

# on:
#   issue_comment:
#     types: [created]

# permissions:
#   contents: read
#   pull-requests: read
#   issues: write

# jobs:
#   chatgpt:
#     if: |
#       github.event.issue.pull_request != null &&
#       github.actor == 'kotahukka' &&
#       contains(github.event.comment.body, 'chatgpt')
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v5
#         with:
#           fetch-depth: 0

#       - name: Run documentation drift guard
#         run: python3 scripts/check_no_mkdocs.py

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: "3.11"

#       - name: Install dependencies
#         run: pip install requests

#       - name: Run ChatGPT review
#         env:
#           OPENAI_TOKEN: ${{ secrets.OPENAI_TOKEN }}
#           PR_NUMBER: ${{ github.event.issue.number }}
#           REPO: ${{ github.repository }}
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: python scripts/chatgpt_review.py

#       - name: Add summary
#         run: |
#           echo "## ChatGPT Review Complete" >> "$GITHUB_STEP_SUMMARY"
#           echo "Triggered by @${{ github.actor }}" >> "$GITHUB_STEP_SUMMARY"
