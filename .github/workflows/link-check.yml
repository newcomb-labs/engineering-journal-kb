# ---
# name: link-check

# "on":
#   pull_request:
#   push:
#     branches:
#       - main
#   schedule:
#     - cron: "0 8 * * 1"
#   workflow_dispatch:

# permissions:
#   contents: read

# jobs:
#   lychee:
#     name: Check documentation links
#     runs-on: ubuntu-latest

#     permissions:
#       contents: read
#       issues: write

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v5

#       - name: Run documentation drift guard
#         run: python3 scripts/check_no_mkdocs.py

#       - name: Run Lychee link checker
#         id: lychee
#         uses: lycheeverse/lychee-action@v2
#         with:
#           args: >
#             --config ./.lychee.toml
#             --format markdown
#             ./docs ./website
#           output: ./lychee-report.md
#           fail: false

#       - name: Debug Lychee outputs
#         if: always()
#         run: |
#           echo "exit_code=${{ steps.lychee.outputs.exit_code }}"
#           echo "stderr<<EOF"
#           echo "${{ steps.lychee.outputs.stderr }}"
#           echo "EOF"
#           echo "stdout<<EOF"
#           echo "${{ steps.lychee.outputs.stdout }}"
#           echo "EOF"
#           echo "Workspace files:"
#           pwd
#           find . -maxdepth 3 -type f | sort

#       - name: Upload Lychee report artifact
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: lychee-report-${{ github.run_id }}
#           path: ./lychee-report.md
#           if-no-files-found: ignore

#       - name: Add report to workflow summary
#         if: always()
#         run: |
#           {
#             echo "## Link Check Report"
#             echo
#             if [ -f ./lychee-report.md ]; then
#               cat ./lychee-report.md
#             else
#               echo "No link report was generated."
#               echo
#               echo "- Lychee exit code: ${{ steps.lychee.outputs.exit_code }}"
#             fi
#           } >> "$GITHUB_STEP_SUMMARY"

#       - name: Fail PR and main runs on link errors
#         if: >
#           github.event_name != 'schedule' &&
#           steps.lychee.outputs.exit_code != '0'
#         run: |
#           echo "Link checker detected documentation link errors."
#           exit 1

#       - name: Create weekly issue for scheduled failures
#         if: >
#           github.event_name == 'schedule' &&
#           steps.lychee.outputs.exit_code != '0'
#         env:
#           GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#         run: |
#           if [ ! -f ./lychee-report.md ]; then
#             echo "Report not found; skipping issue creation."
#             exit 0
#           fi

#           TODAY="$(date -u +%F)"
#           TITLE="fix(docs): weekly link check failures - ${TODAY}"
#           RUN_URL="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"

#           {
#             echo "## Summary"
#             echo
#             echo "The scheduled weekly link check detected documentation link failures."
#             echo
#             echo "- Workflow: \`${{ github.workflow }}\`"
#             echo "- Run ID: \`${{ github.run_id }}\`"
#             echo "- Date (UTC): \`${TODAY}\`"
#             echo "- Workflow run: ${RUN_URL}"
#             echo
#             echo "## Full Report"
#             echo
#             cat ./lychee-report.md
#           } > weekly-link-issue.md

#           gh issue create \
#             --title "${TITLE}" \
#             --body-file weekly-link-issue.md \
#             --label "area:ci" \
#             --label "type:fix"

#       - name: Fail scheduled run after issue creation
#         if: >
#           github.event_name == 'schedule' &&
#           steps.lychee.outputs.exit_code != '0'
#         run: |
#           echo "Link checker detected documentation link errors during scheduled run."
#           exit 1
